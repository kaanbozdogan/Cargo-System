public interface UserInterface
{
	/**
	 * Gives the menu selections to the user and repeats it until a valid input is given
	 * @return returns the selection made
	 */
	public String userSelection();
}
